{
  "project_type": "React Native Expo fitness app with bottom tab navigation",
  "language": "JavaScript (React Native)",
  "build_system": "Expo SDK 49, snack.expo.dev compatible originally. Will need expo-dev-client for native modules (MediaPipe, camera).",
  "test_framework": "No tests exist currently. Will use basic syntax validation.",
  "build_command": "Cannot run npm install or build - registry is blocked (403). Use node --check for syntax validation only.",
  "test_command": "node --check on each .js file for syntax validation",
  "verification_instructions": "1. Run node --check on all .js files to confirm valid syntax. 2. Verify all imported modules are referenced in package.json. 3. Verify file structure is consistent.",
  "snapshot_or_generated_files": null,
  "setup_instructions": "npm install (requires network access to npm registry, which is not available in this sandbox)",
  "environment_constraints": "NPM registry access is blocked (403 Forbidden). Cannot install packages, run expo, or build. All validation is AST/syntax-level only. Node.js v22 is available for syntax checking.",
  "contribution_requirements": "Commit changes to fitness-app branch. No main branch exists.",
  "key_patterns": "Bottom tab navigator in App.js, screens in screens/ directory, @expo/vector-icons for icons, StyleSheet for styling, functional components with hooks, green (#4CAF50) as primary color",
  "relevant_files": [
    "App.js",
    "package.json",
    "screens/HomeScreen.js",
    "screens/ProfileScreen.js"
  ],
  "directory_structure": "Flat structure: App.js at root, screens/ for screen components, package.json for dependencies. Will add: services/ for Supabase, utils/ for pose detection logic, components/ for reusable UI."
}
